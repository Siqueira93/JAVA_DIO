public class Boolean {
    public static void main(String[] args) {
        boolean variavel = false;

        // o ! converte o valor da variavel boolean
        // ou melhor ela inverte o valor

        /// pode ser assim tambem
        variavel = !variavel;

        System.out.println(!variavel);
    }    
}
